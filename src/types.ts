export interface MainMenuItemProps {
    title: string;
    url: string;
    icon: string;
}

export interface Project {
    title: string;
    description: string;
    screenshot: string;
    stack: string[];
}